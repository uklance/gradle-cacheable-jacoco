package foo;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Util${padded} {
	private static final Logger logger = LoggerFactory.getLogger(Util${padded}.class);

	public static int doStuff() {
		logger.info("doing stuff ${padded}");
		return ${i};
	}
}